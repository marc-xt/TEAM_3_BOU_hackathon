# Copy to .env for local dev (.env itself is gitignored).

# Base URL of the Django backend. The dashboard accepts this with or without /api.
REACT_APP_API_BASE=http://localhost:8000/api

# 'true' serves mock/dashboard-mock.json instead of hitting the backend.
# Keep this false when testing the live Django API.
REACT_APP_USE_MOCK=false