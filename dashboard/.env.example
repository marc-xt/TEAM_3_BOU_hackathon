# Copy to .env for local dev (.env itself is gitignored).

# Base URL of the Django backend. No trailing slash.
REACT_APP_API_BASE=http://localhost:8000

# 'true' (default) serves mock/dashboard-mock.json instead of hitting
# the backend. Set to 'false' to talk to the live API.
REACT_APP_USE_MOCK=true
